import React from 'react';
import ThemeChange from './ThemeChange/ThemeChange';

const Header = () => {
    return (
        <header>
            <h1>Todo list</h1>
            <ThemeChange />
        </header>
    );
}

export default Header;